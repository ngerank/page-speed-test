# page-speed-test
Test any page’s load speed in real-time using an iframe and CORS proxy. Calculates actual load time and assigns a UX-based performance score. No Lighthouse, no fluff, just fast insights.
